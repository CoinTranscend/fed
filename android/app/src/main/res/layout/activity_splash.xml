<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/colorBackground">

    <ImageView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:src="@drawable/splash_cover"
        android:scaleType="centerCrop"
        android:contentDescription="Fed Dashboard" />

    <!-- Gradient fades to app background color at bottom -->
    <View
        android:layout_width="match_parent"
        android:layout_height="220dp"
        android:layout_gravity="bottom"
        android:background="@drawable/splash_gradient" />

    <!-- Title block sits on the faded-to-white area -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:orientation="vertical"
        android:paddingStart="28dp"
        android:paddingEnd="28dp"
        android:paddingBottom="52dp">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="FED DASHBOARD"
            android:textColor="@color/colorText"
            android:textSize="26sp"
            android:letterSpacing="0.12"
            android:textStyle="bold" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Macro Intelligence · Real-Time"
            android:textColor="@color/colorPrimary"
            android:textSize="13sp"
            android:letterSpacing="0.04"
            android:layout_marginTop="4dp" />

        <TextView
            android:id="@+id/tvVersion"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@color/colorTextSecondary"
            android:textSize="11sp"
            android:layout_marginTop="10dp" />

    </LinearLayout>

</FrameLayout>
